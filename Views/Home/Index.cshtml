<!--Topic: CSS-->
<!--https://www.w3schools.com/css/ -->
@*
Index.cshtml
*@

<!DOCTYPE html>
<html>
<head>
    <title>Home</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            background-image: url('/Images/download.jpg');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .container {
            text-align: center;
            padding: 20px;
            max-width: 600px;
            background-color: rgba(255, 255, 255, 0.8);
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
        }

        .btn {
            padding: 10px 20px;
            font-size: 16px;
            margin: 10px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .btn-green {
            background-color: #28a745;
            color: white;
        }

            .btn-green:hover {
                background-color: #218838;
            }

        .btn-disabled {
            background-color: #d3d3d3;
            color: #6c757d;
            cursor: not-allowed;
        }

        .welcome-text {
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 10px;
        }

        .select-task {
            font-size: 18px;
            margin-bottom: 20px;
            color: #555;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="welcome-text">Welcome</div>
        <div class="select-task">Select a task</div>
        <button class="btn btn-green" onclick="location.href='@Url.Action("Report")'">Report Issue</button>
        <button class="btn btn-green" onclick="location.href='@Url.Action("AllReports")'">View Reports</button>
        <button class="btn btn-green" onclick="location.href='@Url.Action("Feedback")'">Give Feedback</button> 
        <button class="btn btn-disabled" onclick="location.href='@Url.Action("Events")'">Events and Announcements</button>
        <button class="btn btn-disabled" onclick="showComingSoon()">Service Request Status</button>
    </div>

    <script>
        function showComingSoon() {
            alert('Coming Soon');
        }
    </script>
</body>
</html>
